;define pi and radius   
(define pi 3.14)
(define radius 10)
(* pi ( * radius radius))
;define a circumference function using already defined functions
(define circumference ( * pi radius ))
;output the result
circumference

